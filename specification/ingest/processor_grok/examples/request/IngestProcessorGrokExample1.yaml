method_request: GET _ingest/processor/grok
alternatives:
  - language: Python
    code: resp = client.ingest.processor_grok()
  - language: JavaScript
    code: const response = await client.ingest.processorGrok();
  - language: Ruby
    code: response = client.ingest.processor_grok
  - language: PHP
    code: $resp = $client->ingest()->processorGrok();
  - language: curl
    code: 'curl -X GET -H "Authorization: ApiKey $ELASTIC_API_KEY" "http://localhost:9200/_ingest/processor/grok"'
