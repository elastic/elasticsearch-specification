summary: List with detailed status and human readable times
method_request: GET /_reindex?detailed=true&human=true
description: >
  Run `GET /_reindex?detailed=true` to include the per-task `status` block (current
  progress counters) for each running reindex task.

# type: request
