method_request: GET /_cat/health?v=true&format=json
alternatives:
  - language: Python
    code: |-
      resp = client.cat.health(
          v=True,
          format="json",
      )
  - language: JavaScript
    code: |-
      const response = await client.cat.health({
        v: "true",
        format: "json",
      });
  - language: Ruby
    code: |-
      response = client.cat.health(
        v: "true",
        format: "json"
      )
  - language: PHP
    code: |-
      $resp = $client->cat()->health([
          "v" => "true",
          "format" => "json",
      ]);
  - language: curl
    code: 'curl -X GET -H "Authorization: ApiKey $ELASTIC_API_KEY" "http://localhost:9200/_cat/health?v=true&format=json"'
