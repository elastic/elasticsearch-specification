summary: Text-only embedding task
description: Run `POST _inference/embedding/my-text-only-endpoint` to generate embeddings from the example text
method_request: 'POST _inference/embedding/my-text-only-endpoint'
value: |-
  {
    "input": ["The first text", "The second text"]
  }
