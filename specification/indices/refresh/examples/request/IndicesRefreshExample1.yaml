method_request: GET _refresh
alternatives:
  - language: Python
    code: resp = client.indices.refresh()
  - language: JavaScript
    code: const response = await client.indices.refresh();
  - language: Ruby
    code: response = client.indices.refresh
  - language: PHP
    code: $resp = $client->indices()->refresh();
  - language: curl
    code: 'curl -X GET -H "Authorization: ApiKey $ELASTIC_API_KEY" "http://localhost:9200/_refresh"'
