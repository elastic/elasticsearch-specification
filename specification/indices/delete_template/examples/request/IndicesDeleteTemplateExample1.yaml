method_request: DELETE _template/.cloud-hot-warm-allocation-0
alternatives:
  - language: Python
    code: |-
      resp = client.indices.delete_template(
          name=".cloud-hot-warm-allocation-0",
      )
  - language: JavaScript
    code: |-
      const response = await client.indices.deleteTemplate({
        name: ".cloud-hot-warm-allocation-0",
      });
  - language: Ruby
    code: |-
      response = client.indices.delete_template(
        name: ".cloud-hot-warm-allocation-0"
      )
  - language: PHP
    code: |-
      $resp = $client->indices()->deleteTemplate([
          "name" => ".cloud-hot-warm-allocation-0",
      ]);
  - language: curl
    code: 'curl -X DELETE -H "Authorization: ApiKey $ELASTIC_API_KEY" "http://localhost:9200/_template/.cloud-hot-warm-allocation-0"'
