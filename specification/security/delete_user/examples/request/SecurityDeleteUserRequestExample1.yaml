method_request: DELETE /_security/user/jacknich
alternatives:
  - language: Python
    code: |-
      resp = client.security.delete_user(
          username="jacknich",
      )
  - language: JavaScript
    code: |-
      const response = await client.security.deleteUser({
        username: "jacknich",
      });
  - language: Ruby
    code: |-
      response = client.security.delete_user(
        username: "jacknich"
      )
  - language: PHP
    code: |-
      $resp = $client->security()->deleteUser([
          "username" => "jacknich",
      ]);
  - language: curl
    code: 'curl -X DELETE -H "Authorization: ApiKey $ELASTIC_API_KEY" "$ELASTICSEARCH_URL/_security/user/jacknich"'
