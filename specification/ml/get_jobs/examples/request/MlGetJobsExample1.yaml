method_request: GET _ml/anomaly_detectors/high_sum_total_sales
alternatives:
  - language: Python
    code: |-
      resp = client.ml.get_jobs(
          job_id="high_sum_total_sales",
      )
  - language: JavaScript
    code: |-
      const response = await client.ml.getJobs({
        job_id: "high_sum_total_sales",
      });
  - language: Ruby
    code: |-
      response = client.ml.get_jobs(
        job_id: "high_sum_total_sales"
      )
  - language: PHP
    code: |-
      $resp = $client->ml()->getJobs([
          "job_id" => "high_sum_total_sales",
      ]);
  - language: curl
    code: 'curl -X GET -H "Authorization: ApiKey $ELASTIC_API_KEY" "$ELASTICSEARCH_URL/_ml/anomaly_detectors/high_sum_total_sales"'
  - language: Java
    code: |
      client.ml().getJobs(g -> g
          .jobId("high_sum_total_sales")
      );
