@rest_spec_name("ml.delete_calendar_event")
class DeleteCalendarEventRequest extends RequestBase {
  pathParts?: {
    calendar_id: string;
    event_id: string;
  }
  query_parameters?: {
  }
  body?: {
  }
}
